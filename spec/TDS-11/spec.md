# TDS-11: Delete jira branch flow

Generated from Jira on 2026-05-07T23:27:09.488Z.

## Issue Details

| Field | Value |
|---|---|
| Key | TDS-11 |
| Title | Delete jira branch flow |
| Type | Task |
| Status | To Do |
| Priority | Medium |
| Assignee | - |
| Reporter | Roland Abou Younes |
| Labels | - |
| Components | - |
| Created | 2026-05-08T02:26:09.208+0300 |
| Updated | 2026-05-08T02:26:53.810+0300 |

## Description

The new dispatch flow (`.github/workflows/jira-dispatch.yml` + `.github/scripts/jira-dispatch.mjs`) is in production and validated end-to-end (see `github_actions_claude_spec.md`). The legacy create-branch flow is no longer needed.

## Goal

Delete the legacy create-branch flow and any leftover legacy artefacts. Do not touch the new dispatch flow.

## Files to delete

Delete exactly these files:

1. `.github/workflows/jira-branch-readme.yml`
   - Legacy workflow triggered by GitHub `create` events when Jira created a branch.
2. `.github/scripts/jira-branch-automation.mjs`
   - Helper script used only by the legacy workflow above.
3. `spec/TDS-4-ticket-artefacts-in-the-same-folder.md`
   - Legacy flat spec file generated by the old layout. TDS-4 is already Done and has been superseded.

## Files to keep (do NOT touch)

- `.github/workflows/jira-dispatch.yml`
- `.github/workflows/poc-session.yml`
- `.github/scripts/jira-dispatch.mjs`
- `CLAUDE.md`
- `README.md`
- `github_actions_claude_spec.md`
- `spec/<TICKET-ID>/` directories (these are the canonical per-ticket folders for the new flow)
- All `temp/poc/` artefacts

## Acceptance criteria

- After this ticket merges, `git ls-files .github/workflows .github/scripts` returns only `jira-dispatch.yml`, `poc-session.yml`, and `jira-dispatch.mjs`.
- The new dispatch flow continues to work on a follow-up smoke test (re-run any open `claude:answer` ticket and confirm the workflow still passes).
- No new files should be created outside the ticket folder.

## Notes for Claude

- This is a `claude:pr` ticket. Make the deletions on the per-ticket branch `tdf/<this-key>` and let the workflow open the PR.
- Update `spec/<this-key>/plan.md` with the deletion list and rationale.
- Write a short summary to `spec/<this-key>/response.md`: what was deleted, what was kept, any follow-ups.
- Do not modify any other repository files. Do not delete `temp/poc/` (POC validation artefacts are intentional history).
- Reference: `github_actions_claude_spec.md` "Production Implementation" section.

## Comments (0)

_No comments._
